# Auto-Publish

Automatically crossposts messages in watched announcement channels. When a message is sent in a configured announcement channel, the bot publishes it so followers in other servers see it.

## Configuration

Managed in `/config_dashboard` under the Auto-Publish panel (requires `manage_guild`).

Add or remove announcement channels from the watch list.
