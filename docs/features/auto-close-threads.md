# Auto-Close Threads

Closes inactive threads in forum channels after a configured duration.

## How it works

The bot periodically checks watched forum threads. If a thread has been inactive beyond the threshold, it locks and archives the thread. Threads that are already locked get skipped.
